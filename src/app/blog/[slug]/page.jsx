//the file name is in square brackets and the file name is a dynamic route. this means that the file name is a route and the value of the file name is a variable that can be used in the page.

import React from 'react'

const SinglePostPage = () => {
  return (
    <div>SinglePostPage</div>
  )
}

export default SinglePostPage