import React from 'react'

//login page is in the (auth) folder. the parenteses in the folder name is a way to group the pages that are related to authentication. 
//the auth will not be part of the route nor the url. in this case src/app/login/page.jsx will be the login page.

const LoginPage = () => {
  return (
    <div>LoginPage</div>
  )
}

export default LoginPage