//the next js feature is that we can structure our app pages based from the app directory. every file in the app folder is a page and the file name is the route of the page.

const Home = () => {
  return <div>Home</div>;
};

export default Home;