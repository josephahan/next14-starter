//when there is an error in the app, this component will be rendered. this custom error component will be used by next js to render the error page.
"use client"

const Error = () => {
  return (
    <div>Error</div>
  );
}

export default Error;